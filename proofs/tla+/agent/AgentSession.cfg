\* TLC model checker configuration for AgentSession
\* Usage:  tlc AgentSession -config AgentSession.cfg
\* Usage:  apalache-mc check --config=AgentSession.cfg AgentSession.tla

CONSTANTS
    MaxSessions = 3

INIT Init
NEXT Next

\* State invariants — checked at every reachable state
INVARIANTS
    TypeInvariant
    ForwardOnly
    PlanConsistency

\* Temporal properties — checked across full behaviours
PROPERTIES
    TerminalAbsorbing
    AllSessionsTerminate
